<%layout('layout') -%>

	<style>
                label{
                        text-align:center;
                }
                .login{
                text-align:center;
                }
                form{
                        margin-top:5%;
                }
        </style>
	<form action="/update" method="post">
	<%
		for(var i=0;i<rows.length;i++){%>
	
	<div class="form-group row">
        <label for="example-search-input" class="col-2 col-form-label">닉네임 수정</label>
                <div class="col-10">
                        <input class="form-control" name="phone" type="text" value="<%=rows[i].phone%>" id="phone">
                </div>
         </div>
	<%}%>
	<div class="form-group login">
	<button type="submit" class="btn btn-primary">수정</button>
	</div>
	</form>
